extends layout

block content
  h1= title
  p Welcome to #{title}
  p message #{message}
    div#retour reponse
    
    
  button.shortcut
    i.icon-rocket
      Rocket
      
  div
    button#btn(class='btn btn-success', type='on', name='A12') ON
    button#btn2(class='btn btn-success', type='off', name='A12') OFF

  div
    h2 Json
    p reponse : #{type_piece}

    hr
    for item in personnes
        - var piece = item.piece
        if piece==type_piece 
          p #{piece}
          p #{item.type_module}
          p #{item.code}
        
